
 <% if current_user %>
    <script>
        window.currentUser = {
          "id": <%= current_user.id %>,
          "email": "<%= current_user.email %>", 
          "firstName": "<%= current_user.first_name %>", 
          "lastName": "<%= current_user.last_name %>", 
          "role": "<%= current_user.role %>"
        }; 
    </script> 
<% end %>

<main id="root">Do not see me</main>