<!DOCTYPE html>
<html>
  <head>
    <title>Oenana</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <% if current_user %>
      <!-- <script>
        window.currentUser = {
          "id": <%#= current_user.id %>,
          "email": "<%#= current_user.email %>", 
          "firstName": "<%#= current_user.first_name %>", 
          "lastName": "<%#= current_user.last_name %>", 
          "role": "<%#= current_user.role %>"
        }; 
      </script> -->
       <script type="text/javascript">
          window.currentUser = <%= render("api/users/user.json.jbuilder", user: current_user).html_safe %></script>
    <% end %>

  </head>

  <body>
    <%= yield %>
  </body>
</html>
